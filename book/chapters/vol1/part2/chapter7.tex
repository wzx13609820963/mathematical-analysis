\chapter{Cauchy 数列}
\label{chap:cauchy-criterion}

有时可以证明数列的后两项任意接近，却无法预先说出它们接近哪个数。Cauchy 条件正是这种内部判据。在实数中，完备性保证满足该条件的数列必有极限；\chapterref{chap:real-number-system}构造的有理 Cauchy 列表明，同一推论在 \(\Q\) 中不成立。

\section{Cauchy 条件的量词结构}

\subsection{“项与项接近”代替“项与极限接近”}


若 \(a_n\to a\)，则对充分大的 \(m,n\)，
\[
 \abs{a_m-a_n}
 \le\abs{a_m-a}+\abs{a_n-a}
\]
很小。所以收敛列的尾部各项彼此接近。反过来，若所有后项彼此接近，就得到一个不出现未知极限的判别条件。

“相邻项接近”仍不够。调和部分和满足
\[
 H_{n+1}-H_n=\frac1{n+1}\to0,
\]
但 \(H_n\to+\infty\)。Cauchy 条件要求任意两个充分靠后的项接近，而非只控制相邻项。

\begin{example}
若存在非负数列 \(r_k\) 使
\[
 \abs{a_{k+1}-a_k}\le r_k,\qquad
 \sup_{m>n}\sum_{k=n}^{m-1}r_k\longrightarrow0,
\]
则
\[
 \abs{a_m-a_n}\le\sum_{k=n}^{m-1}r_k
\]
说明 \((a_n)\) 具有 Cauchy 性。几何误差 \(r_k=Cq^k\) 是典型情形。
\end{example}

\subsection{Cauchy 数列的定义与量词结构}


\begin{definition}
实数列 \((a_n)\) 称为 \term{Cauchy 数列}，如果
\[
\forall\varepsilon>0\ \exists N\in\Npos\
 \forall m,n\ge N,\qquad
 \abs{a_m-a_n}<\varepsilon.
\]
\end{definition}

定义要求的是尾部直径趋于零，不是“项的变化越来越慢”。调和部分和的相邻差趋零，但从第 \(n\) 项累积到第 \(2n\) 项仍约为 \(\log 2\)，因而两个晚期项可以保持固定距离。检验 Cauchy 性时必须估计任意 \(m>n\) 的总变化，而不能停在 \(a_{n+1}-a_n\)。

\begin{insightbox}[同一个起点控制两个指标]
Cauchy 条件中的同一个 \(N\) 必须同时控制两个独立指标 \(m,n\)。这表示从第 \(N\) 项起，尾部任意两项都彼此接近，而非只比较相邻项。
\end{insightbox}

把条件写成
\[
 \forall\varepsilon>0\ \forall m,n\ \exists N\le\min\{m,n\}
\]
会失去尾部统一性。严格否定为
\[
\exists\varepsilon_0>0\ \forall N\in\Npos\
 \exists m,n\ge N,\qquad
 \abs{a_m-a_n}\ge\varepsilon_0.
\]

\begin{theorem}[Cauchy 条件的等价形式]
令
\[
 d_N=\sup\{\abs{a_m-a_n}:m,n\ge N\}\in[0,+\infty].
\]
下列条件等价：
\begin{enumerate}
 \item \((a_n)\) 是 Cauchy 列；
 \item 对每个 \(\varepsilon>0\)，存在 \(N\)，使
 \[
   m\ge n\ge N\quad\Longrightarrow\quad
   \abs{a_m-a_n}<\varepsilon;
 \]
 \item 对每个 \(k\in\Npos\)，存在 \(N_k\)，使
 \[
   m,n\ge N_k\quad\Longrightarrow\quad
   \abs{a_m-a_n}<2^{-k};
 \]
 \item \(d_N\to0\)。
\end{enumerate}
\end{theorem}

\begin{proof}
\((1)\Rightarrow(2)\) 是缩小量词范围；\((2)\Rightarrow(1)\) 则在
\(m<n\) 时交换两个指标。\((1)\Rightarrow(3)\) 由定义直接得到。
反之，给定 \(\varepsilon>0\)，取 \(k\) 使
\(2^{-k}<\varepsilon\)，再用相应的 \(N_k\)，得到 \((1)\)。

若 \((1)\) 成立，对 \(\varepsilon/2\) 取尾部，则
\(d_N\le\varepsilon/2<\varepsilon\)，所以 \(d_N\to0\)。若
\(d_N\to0\)，取 \(N\) 使 \(d_N<\varepsilon\)，尾部任意两项的距离都不超过 \(d_N\)，从而得到 \((1)\)。
\end{proof}

对有界列，尾部直径等于
\[
 d_N=\sup_{k\ge N}a_k-\inf_{k\ge N}a_k,
\]
所以\chapterref{chap:limsup-liminf}的尾部振幅判据也是 Cauchy 判据。

\section{收敛蕴含 Cauchy 及 Cauchy 列有界}

\subsection{收敛数列必为 Cauchy 数列}


\begin{theorem}
若实数列 \(a_n\to a\)，则 \((a_n)\) 是 Cauchy 列。
\end{theorem}

\begin{proof}
给定 \(\varepsilon>0\)，取 \(N\) 使
\[
 n\ge N\quad\Longrightarrow\quad
 \abs{a_n-a}<\frac\varepsilon2.
\]
于是对任意 \(m,n\ge N\)，
\[
 \abs{a_m-a_n}
 \le\abs{a_m-a}+\abs{a_n-a}<\varepsilon.
\]
\end{proof}

证明只使用三角不等式，不使用实数完备性。因此它在任意度量空间中都成立。误差 \(\varepsilon/2\) 是把总距离预算分给两项与极限之间的距离。

\begin{corollary}
Cauchy 列的任意子列仍是 Cauchy 列。若一个 Cauchy 列有收敛子列，则原列收敛到同一极限。
\end{corollary}

\begin{proof}
子列继承性由其指标最终进入原列尾部。若 \(a_{n_k}\to a\)，给定 \(\varepsilon>0\)，先用 Cauchy 性使原列尾部任意两项距离小于 \(\varepsilon/2\)，再选同一尾部中的子列项使其与 \(a\) 的距离小于 \(\varepsilon/2\)。三角不等式给出原列整个尾部与 \(a\) 的距离小于 \(\varepsilon\)。
\end{proof}

\subsection{Cauchy 数列必有界}


\begin{theorem}
每个 Cauchy 实数列都有界。
\end{theorem}

\begin{proof}
在定义中取 \(\varepsilon=1\)，得到 \(N\)，使 \(m,n\ge N\) 时
\(\abs{a_m-a_n}<1\)。固定 \(n=N\)，则对 \(m\ge N\)，
\[
 \abs{a_m}\le\abs{a_N}+1.
\]
再把前 \(N-1\) 项的绝对值并入有限最大值，便得到全列统一界。
\end{proof}

逆命题不成立，\((-1)^n\) 是有界非 Cauchy 列。Cauchy 性还比“相邻差趋零”强，因为它控制任意长度的尾部累积误差。

\begin{proposition}
若 \((a_n)\)、\((b_n)\) 为 Cauchy 列，则它们的和、差、积仍为 Cauchy 列；若存在 \(\delta>0\) 使 \(\abs{b_n}\ge\delta\) 最终成立，则 \(a_n/b_n\) 也是 Cauchy 列。
\end{proposition}

\begin{proof}
和、差由三角不等式。乘积使用 Cauchy 列有界性和分解
\[
 a_mb_m-a_nb_n=a_m(b_m-b_n)+b_n(a_m-a_n).
\]
商先证明倒数列：
\[
 \abs{\frac1{b_m}-\frac1{b_n}}
 \le\delta^{-2}\abs{b_m-b_n},
\]
再用乘积结论。
\end{proof}

\begin{proposition}[趋零扰动不改变 Cauchy 性]
若 \(\abs{a_n-b_n}\to0\)，则 \((a_n)\) 是 Cauchy 列，当且仅当
\((b_n)\) 是 Cauchy 列。
\end{proposition}

\begin{proof}
设 \((a_n)\) 是 Cauchy 列。给定 \(\varepsilon>0\)，在一个共同尾部上使
\[
 \abs{a_n-b_n}<\frac{\varepsilon}{3},
 \qquad
 \abs{a_m-a_n}<\frac{\varepsilon}{3}.
\]
于是
\[
 \abs{b_m-b_n}
 \le\abs{b_m-a_m}+\abs{a_m-a_n}+\abs{a_n-b_n}
 <\varepsilon.
\]
反向交换两列即可。
\end{proof}

\section{实数中的 Cauchy 收敛准则}

这一准则有两个方向，地位并不相同。收敛推出 Cauchy 只靠三角不等式，在任何度量空间中都成立；Cauchy 推出收敛则断言空间没有缺失极限，是实数完备性的体现。以后说一个空间完备，指的正是第二个方向在该空间成立。


\begin{theorem}[Cauchy 收敛准则]
实数列收敛，当且仅当它是 Cauchy 数列。
\end{theorem}

\begin{proof}
正向已在上一节证明。反向设 \((a_n)\) 为 Cauchy 列。它有界，且尾部直径满足
\[
 d_N=\sup_{k\ge N}a_k-\inf_{k\ge N}a_k\longrightarrow0.
\]
因此
\[
 \limsup a_n-\liminf a_n=0.
\]
上下极限都是有限实数，\chapterref{chap:limsup-liminf}的收敛判据便说明原列收敛。
\end{proof}

下一章比较各种完备性命题时，还会先用 Bolzano--Weierstrass 定理抽取候选极限，再由 Cauchy 性提升为全列收敛。这里的证明则把尾部直径压缩为上下极限之差。两种证明调用的前置结果不同，结论相同。

\section{有理 Cauchy 列的无理极限}


\chapterref{chap:real-number-system}构造了有理数列
\[
 a_n=\frac{k_n}{10^n},\qquad
 a_n^2<2\le(a_n+10^{-n})^2,
\]
并证明
\[
 0\le a_m-a_n<10^{-n}\qquad(m\ge n).
\]
因此它是 \(\Q\) 中的 Cauchy 列，却没有有理极限。

\begin{theorem}
“每个 Cauchy 列收敛”不是有序域公理的结论，也不是 Archimedes 性质的结论。
\end{theorem}

\begin{proof}
\(\Q\) 是 Archimedes 有序域。前面构造的有理 Cauchy 列若在 \(\Q\) 中收敛到 \(q\)，平方极限定理给出 \(q^2=2\)，与 \(\sqrt2\notin\Q\) 矛盾。
\end{proof}

同一数列在 \(\R\) 中收敛到 \(\sqrt2\)。Cauchy 性只使用项之间的距离，所以在 \(\Q\) 与 \(\R\) 中完全相同；“极限属于哪个空间”则取决于值域是否包含缺失边界。

\section{快速 Cauchy 子列与级数方法}

\begin{proposition}[快速 Cauchy 子列]
每个 Cauchy 列 \((a_n)\) 都有子列 \((a_{n_k})\) 满足
\[
 \abs{a_{n_{k+1}}-a_{n_k}}<2^{-k}\qquad(k\in\Npos).
\]
令 \(d_k=a_{n_{k+1}}-a_{n_k}\)。对每个 \(m\ge2\)，有限和恒等式
\[
 a_{n_m}=a_{n_1}+\sum_{k=1}^{m-1}d_k.
\]
成立，并且该子列为 Cauchy 列，因而收敛。原 Cauchy 列也收敛到同一极限。
\end{proposition}

\begin{proof}
对每个 \(k\)，由 Cauchy 条件取 \(N_k\)，使 \(m,n\ge N_k\) 时 \(\abs{a_m-a_n}<2^{-k}\)。递归选取
\[
 n_{k+1}>n_k,\qquad n_k\ge N_k.
\]
于是所需相邻差估计成立。若 \(m>\ell\)，有限次三角不等式给出
\[
 \abs{a_{n_m}-a_{n_\ell}}
 \le \sum_{k=\ell}^{m-1}\abs{d_k}
 <\sum_{k=\ell}^{m-1}2^{-k}
 <2^{1-\ell}.
\]
给定 \(\varepsilon>0\)，取 \(L\) 使 \(2^{1-L}<\varepsilon\)，便知 \((a_{n_k})\) 是 Cauchy 列。由实数的 Cauchy 收敛准则，该子列收敛。上式中的有限和逐项消去后正是 \(a_{n_m}-a_{n_1}\)，故所列恒等式成立。Cauchy 列若有收敛子列，则全列收敛到同一点。
\end{proof}

这项构造把 Cauchy 列化为一列具有可控有限尾和的增量。第 22 章建立级数以后，可以把同一论证写成“增量级数绝对收敛”。届时，绝对收敛级数的部分和为 Cauchy 列，关键仍是有限尾和估计
\[
 \abs{\sum_{k=n+1}^{m}d_k}
 \le\sum_{k=n+1}^{m}\abs{d_k}.
\]
这里没有把尚未建立的级数收敛定理作为当前证明的前提。

\begin{proposition}[增量尾和判据]
设 \(r_k\ge0\)，并且
\[
 R_N:=\sup_{m>n\ge N}\sum_{k=n}^{m-1}r_k\longrightarrow0.
\]
若
\[
 \abs{a_{k+1}-a_k}\le r_k\qquad(k\in\Npos),
\]
则 \((a_n)\) 是 Cauchy 列。设其极限为 \(L\)，则还得到后验界
\[
 \abs{a_n-L}\le
 \sup_{m>n}\sum_{k=n}^{m-1}r_k.
\]
\end{proposition}

\begin{proof}
对 \(m>n\ge N\)，有限次三角不等式给出
\[
 \abs{a_m-a_n}
 \le\sum_{k=n}^{m-1}\abs{a_{k+1}-a_k}
 \le\sum_{k=n}^{m-1}r_k
 \le R_N.
\]
由 \(R_N\to0\) 得 Cauchy 性。固定 \(n\)，令 \(m\to\infty\)，绝对值保持极限，得到
\[
 \abs{L-a_n}
 \le\sup_{m>n}\sum_{k=n}^{m-1}r_k.
\]
\end{proof}

相邻差趋零只说明单步变化变小；增量尾和判据要求任意长度的后期累积变化一同趋零。调和部分和在这一区别上失败，因为其从 \(n\) 到 \(2n\) 的增量始终不小于固定正数。对 \(r_k=Cq^k\)（\(0<q<1\)），有限几何和给出显式尾界
\[
 \sup_{m>n}\sum_{k=n}^{m-1}r_k
 \le\frac{Cq^n}{1-q}.
\]

本章只在实数距离下讨论 Cauchy 列。一般度量空间中的完备性、闭子空间和完备化将在后文分别定义和证明；这里已经出现的两个原型是：\(\R\) 中每个 Cauchy 列都有极限，而 \(\Q\) 中存在指向 \(\sqrt2\) 的缺口的 Cauchy 列。\chapterref{chap:cauchy-construction}把所有这类有理 Cauchy 列按“差为零列”取等价类，正是完备化构造的具体模型。

\section*{习题}
\addcontentsline{toc}{section}{习题}

\subsection*{A组　定义与基本方法}
\begin{enumerate}[label=\textbf{\thechapter.\arabic*.}]
 \exerciseitem{13.1} 写出 Cauchy 条件的严格否定，并用于证明 \((-1)^n\) 不是 Cauchy 列。
 \exerciseitem{13.2} 证明收敛列为 Cauchy 列，标明 \(\varepsilon/2\) 的作用。
 \exerciseitem{13.3} 证明 Cauchy 列有界，完整处理有限首项。
 \exerciseitem{13.4} 证明 Cauchy 列的任意子列仍为 Cauchy 列。
 \exerciseitem{13.5} 证明 Cauchy 列若有收敛子列，则原列收敛到同一极限。
 \exerciseitem{13.6} 证明尾部直径趋零与 Cauchy 条件等价。
 \exerciseitem{13.7} 给出相邻差趋零但非 Cauchy 的数列，并从定义验证失败。
\end{enumerate}

\subsection*{B组　证明与反例}
\begin{enumerate}[label=\textbf{\thechapter.\arabic*.},resume]
 \exerciseitem{13.8} 证明两个 Cauchy 列的和与积仍为 Cauchy 列。
 \exerciseitem{13.9} 若 \(a_n\) 是 Cauchy 列且最终 \(\abs{a_n}\ge\delta>0\)，证明 \(1/a_n\) 是 Cauchy 列。
 \exerciseitem{13.10} 证明若 \(\abs{a_{n+1}-a_n}\le2^{-n}\)，则 \((a_n)\) 为 Cauchy 列。
 \exerciseitem{13.11} 推广上一题：若非负级数尾和满足
 \[
 \sup_{m>n}\sum_{k=n}^{m-1}r_k\to0
 \]
 且 \(\abs{a_{k+1}-a_k}\le r_k\)，证明 \(a_n\) 为 Cauchy 列。
 \exerciseitem{13.12} 使用 Bolzano--Weierstrass 定理证明实数 Cauchy 准则。
 \exerciseitem{13.13} 对\chapterref{chap:real-number-system}十进制逼近 \(\sqrt2\) 的有理列，直接验证 Cauchy 性并证明无有理极限。
 \exerciseitem{13.14} 构造 \((0,1)\) 中没有区间内极限的 Cauchy 列。
 \exerciseitem{13.15} 证明实数中的闭区间完备，开区间一般不完备。
 \exerciseitem{13.16} 判断“每个有界数列都是 Cauchy 列”“每个有收敛子列的数列都是 Cauchy 列”的真伪。
 \exerciseitem{13.17} 证明 Cauchy 列的上下极限相等，并由此再证 Cauchy 收敛准则。
\end{enumerate}

\subsection*{C组　综合问题}
\begin{enumerate}[label=\textbf{\thechapter.\arabic*.},resume]
 \projectexerciseitem{13.18} \ 【预计用时：75分钟】从 Cauchy 列抽取满足
 \(\abs{a_{n_{k+1}}-a_{n_k}}<2^{-k}\) 的快速子列，并不经过 Bolzano--Weierstrass 定理完成收敛证明。
 \exerciseitem{13.19} 设 \(F\subseteq\R\)。证明以下两项等价：
 \begin{enumerate}[label=(\arabic*)]
  \item 每个取值于 \(F\) 且在 \(\R\) 中收敛的数列，其极限仍属于 \(F\)；
  \item 每个取值于 \(F\) 的 Cauchy 列都收敛到 \(F\) 中的点。
 \end{enumerate}
 并说明这给出了后文“闭子集完备”的实数原型。
 \optionalexerciseitem{13.20} 给出一个数列，它在通常距离下是 Cauchy 列；把距离改为离散距离后不再是 Cauchy 列。
 \optionalexerciseitem{13.21} 若两个实数列满足 \(\abs{a_n-b_n}\to0\)，证明其中一个为 Cauchy 列当且仅当另一个为 Cauchy 列。
 \optionalexerciseitem{13.22} 证明 Cauchy 列的有限线性组合仍为 Cauchy 列，并讨论无穷线性组合为何需要额外一致控制。
 \openexerciseitem{13.23} 比较“序完备”“Cauchy 完备”和“区间套完备”在 Archimedes 有序域与一般度量空间中的关系。
 \projectexerciseitem{13.24} \ 【完成\chapterref{chap:cauchy-construction}后回做；预计用时：90分钟】概括 \(\Q\) 的 Cauchy 完备化：等价关系、常值嵌入、稠密性和完备性的四个证明模块。
\end{enumerate}

\section*{本编综合习题}
\addcontentsline{toc}{section}{本编综合习题}

\begin{enumerate}[label=\textbf{综.\arabic*.}]
 \exerciseitem{II.1} 对命题“\(a_n\to a\)”依次完成：写出定义、写出否定、写出邻域形式、证明有限项修改不影响结论。
 \exerciseitem{II.2} 设 \(a_n=(n+\alpha)/(n+\beta)\)。讨论分母最终非零，给出极限及关于参数 \((\alpha,\beta)\) 在有界矩形上的一致误差估计。
 \exerciseitem{II.3} 设 \(a_n,b_n\) 收敛。证明线性组合、乘积、商、绝对值和最大值的极限定理，并为每个必要条件给出一个删去后失败的例子。
 \exerciseitem{II.4} 分析递推 \(a_{n+1}=(a_n+3/a_n)/2\)、\(a_1>0\)：不变区间、单调性、有界性、极限与误差估计。
 \exerciseitem{II.5} 对有界数列完成三种抽取：收敛子列、单调子列、趋于上极限的子列；比较三种构造的输入与输出。
 \exerciseitem{II.6} 构造一个部分极限集合恰为 \(\{-2,0,1\}\) 的数列，计算上下极限、尾部振幅，并判断是否 Cauchy。
 \exerciseitem{II.7} 证明下列条件等价：\(a_n\to L\)；每个子列都有进一步子列趋于 \(L\)；\(\liminf a_n=\limsup a_n=L\)；尾部振幅趋零且存在一条子列趋于 \(L\)。
 \exerciseitem{II.8} 对 \(\sum_{k=1}^n2^{-k}u_k\)（\(\abs{u_k}\le1\)）证明 Cauchy 性、统一有界性及尾部误差，并讨论 \(u_k\) 不收敛是否妨碍部分和收敛。
 \projectexerciseitem{II.9} \ 【预计用时：90分钟】从确界原理出发重建链条：单调有界收敛、区间套、Bolzano--Weierstrass、Cauchy 准则。
 \exerciseitem{II.10} 比较 \(\Q\) 与 \(\R\) 中同一个有理 Cauchy 列：Cauchy 性、可能极限、值域闭性与完备性的差异。
 \optionalexerciseitem{II.11} 构造有界列 \(a_n,b_n\)，使二者各有两个部分极限，而 \(a_n+b_n\) 分别出现一个、两个和三个部分极限的三组例子。
 \openexerciseitem{II.12} 以“尾部”为主线总结第二编：最终性质、完整尾部、尾部值集、尾部确界、尾部直径分别解决什么问题，哪些信息彼此可恢复。
\end{enumerate}

\section*{部分习题提示}
\begin{itemize}
 \item \exerciseref{13.7}：使用调和部分和。
 \item \exerciseref{13.17}：对 Cauchy 列，尾部上、下确界之差不超过尾部直径。
 \item \exerciseref{13.20}：离散距离为 \(d(x,y)=1\)（\(x\ne y\)）。
 \item 综合题II.4：这是 \(\sqrt3\) 的 Newton 迭代。
 \item 综合题II.8：任意尾和绝对值不超过几何尾和。
\end{itemize}
