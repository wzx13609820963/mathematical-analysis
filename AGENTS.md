# 数学分析教材工程协作规则

## 项目定位

本仓库编写《从高等数学到数学分析：严格基础、经典理论与现代分析》。读者已学过工科高等数学和基础线性代数。正文从严格数学分析逐步进入实分析、复分析、拓扑、概率、调和分析、泛函分析、几何分析与微分方程。

## 开始工作前分层读取

普通项目文件不会被自动读入。先判断任务类型，再主动读取足够而不过量的材料。

### 每个任务必读

1. `教材写作规范.md` 中与当前任务对应的条款；
2. `教材总目录.md` 中本章、本节以及直接相邻条目；
3. 当前要修改的源文件；
4. `docs/NOTATION.md`、`docs/TERMINOLOGY.md` 和 `docs/DEPENDENCIES.md` 中与当前对象有关的条目。

### 按任务追加读取

- 新写整章或大幅改写：读取前言、最近的直接前置章节和第0章；
- 修改定理或证明：读取依赖表所列前置结果及所有直接引用处；
- 修改习题或解答：读取同章正文、对应解答及相邻题目体例；
- 修改环境、编号、链接或构建：读取 `book/analysisbook.sty`、`book/main.tex`、`book/solutions.tex` 和相应构建脚本；
- 只改局部文字、错字或单个公式：无须读取前言、整本主文件或无关章节。

若登记表尚无相应条目，应先从源文件核实，不得凭记忆补造依赖或符号含义。

## 权限边界

- `教材总目录.md`、`教材写作规范.md`、`AGENTS.md` 和全书级排版方案由宏观设计对话维护。
- 具体内容对话不得修改这些文件，不得自行增删、移动或重命名卷、编、章。
- 若发现目录、规范或排版存在问题，只在交付说明中列出建议，不直接修改。
- 第0章是当前风格样章。后续章节应继承其严格程度和客观语气，但不得机械复制其段落模板。

## 章节写作要求

- 先核对前置定义和允许引用的定理，避免循环引用。
- 给出概念产生的具体问题，再给严格定义；解释不得替代证明。
- 主要定理给出完整或接近完整的证明，并指出关键条件的使用处。
- 配置正例、边界例和反例；反例须核对全部假设。
- 不重复高等数学中的基础算法，除非计算揭示结构或适用边界。
- 只在确有统一作用时引入更高层观点。
- 正文使用客观教材语气。避免成串反问、口号式“不是……而是……”、反复的“真正”“本质”“核心”、空泛排比和生成式过渡语。
- 不要让章首预告、提示框、常见误解和章末小结重复同一段内容。
- 彩色提示框只用于少量关键警告或旁注；普通辨析和章末小结采用无框正文，具体密度遵守写作规范。

## 习题与解答

- 每章必须有分层习题；重要章节提高题量、证明强度和分步项目比例。
- 题目应覆盖定义、证明、反例、技术训练、结构迁移和经典结果重建。
- 每道正式习题必须在 `book/solutions/` 的对应文件中有详细解答，编号完全一致。
- 正文习题使用 `\exerciseitem{<稳定键>}`，解答使用相同稳定键的 `\begin{solution}{<稳定键>}`；显示题号由 LaTeX 计数器和交叉引用生成，不得在解答标题中手写题号。
- 正文只保留必要提示，详细答案不得写入正文文件。

## 文件与构建

- 正文路径：`book/chapters/volN/partN/chapterN.tex`。
- 解答路径：`book/solutions/volN/partN/chapterN-solutions.tex`。
- 文件编辑使用补丁方式，保留用户已有修改，不清理无关文件。
- 完成后分别运行 `book/build.ps1` 与 `book/build-solutions.ps1`。
- 构建前运行 `book/check-project.ps1`；不得保留硬编码的跨章或跨习题显示编号。
- 检查编译日志中的错误、未定义引用、公式越界和字体问题，并对生成 PDF 做页面渲染抽查；涉及排版改动时须逐页检查受影响页面。

## 校核任务

- 新完成范围的第一次正式校核必须只审查、不修改被审正文、解答、样式、目录、主文件和构建脚本。
- 首次校核只可读取、静态检查、在任务临时目录编译或渲染，并在 `docs/审查报告/` 下对应的卷、编、章或综合范围目录中生成 `YYYY-MM-DD-首次校核.md`。
- 报告必须记录版本快照、实际检查范围、问题级别、文件与行号、未完成检查及建议整改顺序；抽查不得表述为全量检查。
- 整改和复核另开轮次。首次报告不得被后续结果覆盖，也不得在首次校核中顺手修正问题。

## 子任务文件处理

- 每个子任务使用唯一 `task-id`，临时编译、阶段性 PDF、页面渲染、检查日志和候选发布物统一放入 `tmp/tasks/<task-id>/`，不得散落在 `book/`、章节目录或工作区根目录。
- 子任务须记录自己创建和修改的文件；交付前删除自己产生且不再需要的临时文件。不得清理其他任务的目录，也不得删除归属不明的文件。
- `book/main.pdf`、`book/solutions.pdf` 是可再生构建物；正式发布 PDF 只放在 `output/pdf/`，且文件名必须与实际载入范围一致。
- 若正式 PDF 正被占用，停止发布并报告被锁定路径。不得生成 `new`、`copy`、数字后缀等替代 PDF，不得复制或改名 `.tex` 源文件。必要的候选文件只能保留在本任务的 `publish-pending/` 目录，解除占用并成功发布后即清理。
- 文件卫生检查的第一次运行只生成候选清理清单，不执行删除。只有文件可证明为本任务所创建且可再生，或用户批准了清理清单，才可删除。

## 分级验收

- 普通局部修改：全书编译、日志检查，并逐页检查受影响页面；
- 完成或大改一章：全书编译，并逐页检查该章全部页面及前后衔接页；
- 完成一编、一卷或正式发布：全书编译，对该范围逐页视觉检查；正式发布还须核对目录、书签、链接和文件名。
- “逐页检查”只能用于实际逐页查看过的范围；抽查必须明确写成抽查。

## Git 协作

- 开始前检查当前分支、工作区状态和远端差异；不得覆盖其他任务的未提交修改。
- 同一工作区中的章节任务原则上串行执行。若必须并行，使用独立 worktree 和独立分支。
- 每次提交只包含当前章节正文、对应解答和必要的载入文件。
- 未经用户明确要求，不强推、不改写历史、不自动合并存在冲突的分支。

## 交付标准

交付时说明修改文件、完成的数学内容、习题与解答数量、编译和视觉检查结果，以及发现但未擅自处理的宏观目录或规范问题。
